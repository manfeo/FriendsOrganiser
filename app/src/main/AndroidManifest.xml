<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FriendsOrganiser"
        tools:targetApi="31">
        <activity
            android:name=".MainActivityPackage.ChatsPackage.ChattingActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivityPackage.MainActivity.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".MainActivityPackage.MainActivity.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivityPackage.MainActivity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrationLogin.RegisterLoginActivity"
            android:exported="false" />
        <activity
            android:name=".WelcomeActivity.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MainActivityPackage.Messager.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>
</manifest>